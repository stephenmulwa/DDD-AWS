machine:
  node:
    version: 8.9.4
  services:
   - docker

dependencies:
  post:
    - docker build -t ddd-aws .
test:
  post:
    - docker run -d -p 4200:4200 ddd-aws; sleep 15
    
    - curl --retry 10 --retry-delay 5 localhost:4200 | grep "DDD AWS Heroes & Heroines"

    
